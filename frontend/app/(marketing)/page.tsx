/**
 * app/(marketing)/page.tsx
 *
 * This file exists only for Next.js type-validator compliance (every page.tsx
 * in the App Router must export a default React component). The real "/" page
 * is served from app/page.tsx; Next.js prioritises that file over route-group
 * pages when both resolve to the same segment, so this component is never
 * actually rendered.
 */
export default function MarketingGroupPlaceholder() {
  return null;
}
